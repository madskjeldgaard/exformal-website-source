{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build",
      "type": "shell",
      "command": "hugo --cleanDestinationDir --verbose --logLevel info --destination public-website",
    },
    {
      "label": "Publish",
      "type": "shell",
      "command": "cd public-website && git add --all && git commit -m \"autocommit from neovim\" && git push",
      "dependsOn": "Build"
    }
  ]
}
