{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build",
      "type": "shell",
      "command": "hugo --cleanDestinationDir --verbose --logLevel info --destination public",
    },
    {
      "label": "Publish",
      "type": "shell",
      "command": "cd public && git add --all && git commit -m \"Publishing to gh-pages\" && git push origin gh-pages",
      "dependsOn": "Build"
    }
  ]
}
