{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build",
      "type": "shell",
      "command": "hugo --cleanDestinationDir --verbose --logLevel info --destination ../exformalrecords.art-public",
    },
    {
      "label": "Publish",
      "type": "shell",
      "command": "cd ../exformalrecords.art-public && git pull && git add --all && git commit -m \"autocommit from neovim\" && git push",
      "dependsOn": "Build"
    }
  ]
}
